services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - app-data:/app/data
    environment:
      - PORT=4000
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  app-data:
